# This folder is no longer used by the app

Products now come from the backend's database, with photos hosted on
Cloudinary and uploaded through the admin panel (`/admin`) — not from local
files here.

Your photos in these category folders are kept as-is in case you want to
re-upload them through the admin panel, but the running site no longer
reads from this directory at all.
