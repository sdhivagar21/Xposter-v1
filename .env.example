# Placeholder API base URL — no backend exists yet. The app runs entirely
# off local mock data (src/data/mockProducts.js). Point this at a real API
# later and wire up src/api/client.js calls; no other code changes needed.
VITE_API_BASE_URL=http://localhost:4000/api
